<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-xl">
        <a href="{{ url_for('index') }}" class="navbar-brand">
            {{ icon }} {{ title }}
        </a>
        {% if user and user.is_admin %}
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="#" class="nav-link">
                    <i class="bi bi-shield-lock-fill extra-spacing"></i> Security
                </a>
            </li>
        </ul>
        {% endif %}
        {% if user %}
        <ul class="navbar-nav ms-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#">
                    <i class="bi bi-person extra-spacing"></i> {{ user.username }}
                </a>
                <ul class="dropdown-menu">
                    <li>
                        <a class="dropdown-item" href="#">
                            <i class="bi bi-person extra-spacing"></i> Profile
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href=" {{ url_for('get_logout') }} ">
                            <i class="bi bi-box-arrow-right extra-spacing"></i> Logout
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
        {% else %}
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a href="{{ url_for('get_login') }}" class="nav-link">
                    <i class="bi bi-box-arrow-in-right extra-spacing"></i> Login
                </a>
            </li>
        </ul>
        {% endif %}
    </div>
</nav>
